server:
  port: 9000

spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
  redis:
    host: 192.168.5.130
    password: root


app:
  threadpool:
    corePoolSize: 8
    maximumPoolSize: 16
    keepAliveTime: 1
    queueSize: 5000
